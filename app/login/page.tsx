"use client";
import {createBrowserClient} from "@supabase/ssr"; import {Flame} from "lucide-react"; import {Button} from "@/components/ui/button";
export default function Login(){const signIn=async()=>{const supabase=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);await supabase.auth.signInWithOAuth({provider:"google",options:{redirectTo:`${location.origin}/auth/callback`}})};return <main className="login-page"><section className="login-card"><span className="login-logo"><Flame/></span><em>PRIVATE GROUP PLANNER</em><h1>Welcome to<br/>Campfire Circle</h1><p>Sign in with your Google account. Your messages, votes and changes will be recorded under your identity.</p><Button onClick={signIn} className="google-button"><b>G</b> Continue with Google</Button><small>One account per person · Secure sign-in · No account switching inside the app</small></section></main>}
